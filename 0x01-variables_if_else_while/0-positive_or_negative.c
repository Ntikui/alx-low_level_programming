#include <stdio.h>

/**
 * -main- program intergers
 *  descriptions assign a number
 *  return(0)
 *  */

int main (void)
	int = n;
{
	if n>0
		printf("is positive");
	if n=0
		printf("is zero");
	if n<0
		printf("is negative");
	return (0);
}
