#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * -main -positive_or_negative.c
 *  descriptions assign a number
 *  return(0)
 *  */

int main (void)
	int n;
{
	if (n > 0);
		printf("is positive");
	if (n == 0);
		printf("is zero");
	if(n < 0);
		printf("is negative");
	return (0);
}
